name: herdr_flutter
description: A herdr sidebar that watches, restarts and inspects a running Flutter app beside the agent.
publish_to: none
version: 0.3.0

environment:
  sdk: ^3.8.0

dependencies:
  vm_service: ^15.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
